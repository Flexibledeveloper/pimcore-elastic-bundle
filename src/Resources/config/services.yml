services:
    # default configuration for services in *this* file
    _defaults:
        # automatically injects dependencies in your services
        autowire: true
        # automatically registers your services as commands, event subscribers, etc.
        autoconfigure: true
        # this means you cannot fetch services directly from the container via $container->get()
        # if you need to do this, you can override this setting on individual services
        public: true

    ########################
    # Custom Elastic Client
    ########################
    ElasticBundle\Services\:
        resource: '../../Services'

    Flexibledeveloper\PimcoreElasticBundle\Services\ElasticClientService:
        arguments:
            $configuration: { elasticHost: '%env(ELASTIC_HOST)%', elasticPort: '%env(ELASTIC_PORT)%'}

    Flexibledeveloper\PimcoreElasticBundle\Services\ElasticaConnectionService:
        arguments:
            $configuration: { elasticHost: '%env(ELASTIC_HOST)%', elasticPort: '%env(ELASTIC_PORT)%' }

    Flexibledeveloper\PimcoreElasticBundle\Services\ElasticFilterService:
        arguments:
            $configuration: { configFilePath: '../Resources/config/elastic.yml' }

    Flexibledeveloper\PimcoreElasticBundle\Command\:
        resource: '../../Command'
        tags: [ 'console.command' ]
